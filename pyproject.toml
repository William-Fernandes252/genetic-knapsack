[project]
name = "genetic-knapsack"
version = "0.1.0"
description = "Python implementation of the genetic algorithm for the knapsack problem."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.9.3",
    "pandas>=2.2.3",
]
